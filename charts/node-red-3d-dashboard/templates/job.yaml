apiVersion: batch/v1
kind: Job
metadata:
  name: installer-test
spec:
  template:
    spec:
      initContainers:
      - name: copynodered
        image: busybox
        command: ['/bin/sh', '-c', 'touch text.txt']
        volumeMounts:
        - name: node-red-threejs-volume
          mountPath: /data
      restartPolicy: Never
      volumes:
      - name: node-red-threejs-volume
        emptyDir: {}
  backoffLimit: 0
